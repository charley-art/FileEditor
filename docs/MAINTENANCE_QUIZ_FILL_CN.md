# NCEditor 维护实战测验（填写版）

填写人：`_________dxc_______`  
日期：`2026/5/5__________`  
版本基线：`main / v1.0.0`（可改：`________________`）

---

## 一、基础判断（对/错）

1. 100MB 文件应全量下发到 QML，避免 C++ 复杂度。  
你的答案：`__错__`

2. 编辑主链路应经过 `WorkspaceController` 再进入 `DocumentSession`。  
你的答案：`__正确__`

3. 保存应使用 `QSaveFile` 原子提交，避免断电损坏原文件。  
你的答案：`__正确__`

4. 保存进行中允许继续粘贴，只要最后能保存成功即可。  
你的答案：`__错误__`

5. 查找结果高亮必须全量显示，不能设置上限。  
你的答案：`__错误__`

6. 多文档窗口上限是 4。  
你的答案：`__正确_`

7. `LineIndex` 的职责是“行号 <-> 偏移”映射，不是渲染 UI。  
你的答案：`__正确__`

8. 单次粘贴上限默认是 10KB（可配置）。  
你的答案：`_正确___`

---

## 二、填空题（函数/模块名）

1. 键盘输入第一落点函数：`___keyPressEvent_____________`  
2. 文本编辑进入文档层的关键函数：`____ApplyTextEdit____________`  
3. 真正执行增删改缓冲的模块：`_____PieceTable___________`  
4. 查找输入从 UI 到控制器的入口：`______onQueryChangedDebounced__________`  
5. 保存链路中负责写盘并提交的关键点：`_______beginAsyncSave_________ -> ____writeFile____________`  
6. 关闭窗口行为归属模块：`____WorkSpaceController____________`

---

## 三、场景定位题

每题请写：`责任层 + 第一入口 + 最小改动`

### 题1
现象：100MB 文件查找重复字符时卡顿明显。  
责任层：`______DocumentSession__________`  
第一入口：`_____rebuildSearchCache___________`  
最小改动：`____限制查询个数____________`

### 题2
现象：关闭多个大文件后，再打开 100MB 提示内存不足。  
责任层：`_____WorkSpaceController___________`  
第一入口：`______openFile,closeFile__________`  
最小改动：`____关闭时释放内存____________`

### 题3
现象：保存中用户还能触发内容修改操作。  
责任层：`________DocumentSession________`  
第一入口：`___WorkSpaceController中的canEdit属性_____________`  
最小改动：`_____在保存时将canEdit置为false___________`

### 题4
现象：四宫格里“打开更多”在已有 4 窗时仍创建新窗。  
责任层：`___WorkSpaceController_____________`  
第一入口：`___openMoreFile_____________`  
最小改动：`____在openMoreFile中检查当前窗口个数，小于四不要创建新窗口________`

---

## 四、发布回归排序题

请按推荐顺序填写编号（例如：`A -> B -> C ...`）：

- A 按钮规则
- B 中文输入法
- C 保存并发限制
- D 大文件打开/滚动/编辑
- E 查找替换上限
- F 关闭重开

你的顺序：`______A,D,C,E,B,F_______`

---

## 五、简答题

1. 为什么大文件场景优先“调参数”而不是先改结构？  
你的回答：  
`_因为代码中对参数进行了配置，如果大文件编辑过程中出现问题，可能是配置的参数太小。_____________________________________________`  
`____________________________________________________________`

2. `PieceTableBuffer + LineIndex` 相比整文字符串改写，核心收益是什么？  
你的回答：  
`__不会频繁改动原始文本，且可以快速定位行_______________________________________________________`  
`____________________________________________________________`

---

## 六、提交格式（直接复制下面模板回传）

```text
一、1= ,2= ,3= ,4= ,5= ,6= ,7= ,8=
二、1= ,2= ,3= ,4= ,5= ,6=
三、题1: 责任层= ,入口= ,最小改动=
   题2: 责任层= ,入口= ,最小改动=
   题3: 责任层= ,入口= ,最小改动=
   题4: 责任层= ,入口= ,最小改动=
四、顺序=
五、1= ; 2=
```

